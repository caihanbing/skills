# 修复研发过程管理验证器完整性与新鲜度

<!-- work-id: WORK-20260911-001 -->
<!-- work-title: 修复研发过程管理验证器完整性与新鲜度 -->
<!-- work-type: change -->
<!-- work-status: closed -->
<!-- risk-level: medium -->
<!-- feature-ids:  -->
<!-- created: 2026-09-11 -->
<!-- updated: 2026-09-11 -->
<!-- code-basis: HEAD 25809d372a1f; working-tree=dirty -->
<!-- verification-basis: HEAD 25809d372a1f; worktree-digest sha256:7ce992a17dd073cd62804b582d94adece5a7fff380ad5cd10f300a0141f68019 -->

## 工作概述

本 Work Item 修复 `manage-dev-work` 与 `manage-feature-docs` 的引用完整性、状态证据和 Git 新鲜度校验，并同步当前文档契约。

## 范围与验收

### REQ-001 — 需求摘要

- **需求**：验证器不得对重复 ID、孤立引用、缺失 Feature Doc、过期验证基线或不完整风险证据误放行。
- **验收标准**：新增回归测试覆盖上述场景；两套 Skill 的完整测试与 Python 编译检查通过。
- **实现证据**：`manage-dev-work/scripts/dev_work.py`、`manage-feature-docs/scripts/feature_docs.py`、模板和契约文件已更新。
- **验证证据**：`python3 -m unittest discover -s manage-dev-work/tests`（36/36）；`python3 -m unittest discover -s manage-feature-docs/tests`（35/35）；`python3 -m py_compile ...`。
- **状态**：VERIFIED
- **延期依据**：不适用：未延期。
- **后续工作**：不适用：未延期。

## 变更影响

| 维度 | 影响 |
| --- | --- |
| Code | affected |
| API | none |
| Database | none |
| Configuration | none |
| Cache | none |
| MQ | none |
| Security | none |
| Concurrency | none |
| Observability | none |
| Deployment | none |
| Compatibility | none |
| Transaction | none |
| Performance | none |
| Cross-service | none |

## 决策

### DEC-001 — 待决事项

- **状态**：RESOLVED
- **确认路径**：本次用户已确认采用最小可验证修复设计，并明确不使用隔离 worktree。
- **决定**：仅修复已核验的验证器与契约问题，不引入 IDE、Dashboard、CI 或外部服务。
- **原因**：保持 Skill 的 repository-local、Git-friendly 边界，降低无基线扩张风险。
- **取舍**：暂不实现 last-verified 与历史变更日期的强绑定，避免误伤部分完成/回滚记录。
- **证据**：用户确认消息及两份评估报告的核验结论。

## 风险

### RSK-001 — 风险摘要

- **状态**：MITIGATED
- **确认路径**：代码审查与两套测试套件。
- **概率**：低；改动集中在标准库 Python 验证逻辑。
- **影响**：中；门禁变严格可能阻止原先误放行的文档。
- **缓解措施**：保留旧 Feature Docs 回归段兼容；新增场景均使用独立临时仓库验证。
- **验证**：36 个 Work Item 测试、35 个 Feature Docs 测试及编译检查全部通过。

## 实施计划

- [x] 增加结构、引用、索引和日期门禁。
- [x] 增加 Work Item verification-basis 与 Feature Docs 工作区指纹。
- [x] 修复 fenced Markdown 解析和文档契约冲突。
- [x] 编写并通过对抗性回归测试。

## 实施记录

已完成上述计划；实现只使用 Python 标准库和 Git CLI。

## 验证

### VER-001 — 验证项

- **关联需求**：REQ-001
- **状态**：PASS
- **证据**：公开 CLI 与测试套件覆盖重复 ID、孤立引用、延期引用、缺失 Feature Doc、过期验证基线、fenced Markdown 和索引漂移。

## 代码审查

### REV-001 — 审查结论

- **状态**：PASS
- **范围**：验证顺序、跨文档引用、Git freshness、旧文档兼容性和测试回归。
- **证据**：本次变更 diff 审阅、`git diff --check`、两套测试套件和 `py_compile`。

## 发布与回滚

- **发布策略**：仅发布 Skill 文件；无运行时服务、数据库或配置发布动作。
- **回滚策略**：回退本 Work Item 涉及的代码、模板、契约和测试变更即可。

## 用户回归

- **是否需要用户回归**：否
- **需更新、部署或重启的服务**：无；仅修改仓库内 Skill 与测试文件。
- **无需操作的服务**：全部运行时服务均无需操作。
- **操作顺序**：不适用：没有运行时变更。
- **前置条件**：Python 3 和 Git 可用。
- **回归步骤与预期结果**：执行两套 `unittest`、`py_compile` 和 `git diff --check`；预期全部通过且错误场景被拒绝。
- **用户回归结果**：not-required

## 长期知识提炼

- **Feature Docs**：不适用：本变更维护 Skill 实现，无稳定业务 Feature。
- **提炼结果**：验证器契约变化已记录在本 Work Item 与计划文件中。

## Bug Fix 闭环

- **症状**：不适用：非 bugfix。
- **复现**：不适用：非 bugfix。
- **根因**：不适用：非 bugfix。
- **失败机制**：不适用：非 bugfix。
- **修复**：不适用：非 bugfix。
- **回归保护**：不适用：非 bugfix。
